# Backend API base URL.
# Local dev: leave unset (defaults to http://localhost:8080) or set explicitly.
# Production (Vercel): set to your Railway backend URL, e.g.
#   VITE_API_BASE_URL=https://visitalk-api.up.railway.app
VITE_API_BASE_URL=http://localhost:8080
